# accounts/urls.py
from django.urls import path
# from .views import ...   ← import your views here later

urlpatterns = [
    # Add your account-related paths here later, e.g.
    # path('register/', RegisterView.as_view(), name='register'),
    # path('profile/', ProfileView.as_view(), name='profile'),
    
    # For now, you can leave it empty or add a dummy path to test
    # path('', lambda request: HttpResponse("Accounts app root"), name='accounts-root'),
]